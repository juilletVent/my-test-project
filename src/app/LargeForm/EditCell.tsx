import React from "react";
import { Form } from "antd";
import styled from "styled-components";

interface Props {
  form: any;
}

function EditCell(props: Props) {
  const { form } = props;
  return <>AAA</>;
}

export default EditCell;
